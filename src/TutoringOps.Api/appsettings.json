{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "Oracle": {
    "//": "Set locally with: dotnet user-secrets set \"Oracle:ConnectionString\" \"...\"",
    "//deployed": "In Azure this comes from the app's Connection Strings as 'Oracle'.",
    "ConnectionString": "",
    "CommandTimeoutSeconds": 30
  },

  "ServiceBus": {
    "//": "Leave empty to run without Azure: events are logged instead of sent.",
    "ConnectionString": "",
    "QueueName": "session-events",
    "TopicName": "session-notifications"
  },

  "Outbox": {
    "Enabled": true,
    "PollIntervalSeconds": 5,
    "BatchSize": 50
  },

  "EventGrid": {
    "//": "Stretch feature. Inert unless both endpoint and key are set.",
    "//warning": "A misconfigured topic stalls the whole outbox -- see /ops/outbox.",
    "TopicEndpoint": "",
    "AccessKey": "",
    "EventTypes": [ "PackageExhausted" ]
  },

  "Ops": {
    "//": "Shared key the Function App sends in X-Ops-Key. Set in Azure config.",
    "ApiKey": ""
  },

  "Cors": {
    "AllowedOrigins": []
  }
}
