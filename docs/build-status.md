# Build status

The whole point of the original plan was claim discipline: nothing goes on a
resume until the corresponding checkbox is real. This file keeps that honest.

There are two different questions, and they are tracked separately:

1. **Is it written?** — is the code in this repo.
2. **Has it been run?** — has it been executed against a real Oracle, a real
   Azure subscription, a real family.

Code that is written but never executed is not a rung on any ladder.

---

## Has it been run?

**No. Nothing in this repository has been executed yet.**

It was authored in an environment with no reachable Oracle and no .NET SDK: the
egress policy blocked Docker Hub (so `gvenzl/oracle-xe` could not be pulled) and
every Microsoft download host (so the .NET 8 SDK could not be installed). The
PL/SQL has therefore never been compiled by Oracle, and the C# has never been
compiled by `dotnet build`.

What *was* done instead: a static check across the PL/SQL sources confirming
that every subprogram declared in a package spec has an implementation in its
body, and that every `PKG_X.member` reference in the codebase resolves to
something that package actually declares. That catches the most common class of
compile error and nothing more. Expect to fix real compile errors on the first
run.

**First run should be, in this order:**

```bash
cd db
docker compose up -d
./install.sh          # fails loudly if any package is INVALID
./run_tests.sh        # the business-rule suite
./run_concurrency.sh  # the concurrency suite

cd ../src
dotnet build TutoringOps.sln
dotnet test TutoringOps.sln
```

`install.sh` queries `user_objects` for anything not `VALID` and exits non-zero,
so a package that compiled with errors cannot pass silently.

Update this section as things actually run. Until a line here says it ran, it
has not.

---

## Week 1 — Oracle schema and PL/SQL

| Item | Written | Where |
|---|---|---|
| Oracle XE 21c in Docker | yes | `db/docker-compose.yml` |
| App schema, not `SYSTEM` | yes | container creates `tutoring` in `XEPDB1` |
| `STUDENTS` | yes | `db/migrations/01_schema.sql` |
| `PACKAGES` | yes | same |
| `SESSIONS` | yes | same |
| `PAYMENTS` | yes | same |
| `EVENT_OUTBOX` | yes | same |
| Identity columns for keys | yes | `GENERATED BY DEFAULT ON NULL AS IDENTITY` |
| Foreign keys and status check constraints | yes | same |
| `PKG_SCHEDULING.book_session` | yes | `05_pkg_scheduling.sql` |
| — no double-booking, `FOR UPDATE` on the window | yes | locks the tutor row, then counts overlaps |
| — business hours check | yes | `PKG_VALIDATION.check_business_hours` |
| — requires package hours or an unapplied payment | yes | `reserve_hours` then `apply_payment` |
| — writes `SESSIONS` + `EVENT_OUTBOX` in one transaction | yes | caller commits both |
| `PKG_SCHEDULING.cancel_session` | yes | with the 24-hour split |
| `PKG_SCHEDULING.complete_session` | yes | see the note below |
| `PKG_BILLING.purchase_package` | yes | `04_pkg_billing.sql` |
| `PKG_BILLING.apply_payment` | yes | same |
| `PKG_BILLING.get_balance` | yes | same |
| FIFO: oldest package first | yes | `reserve_hours`, plus a split-across-packages test |
| `PKG_VALIDATION` status transitions | yes | `02_pkg_validation.sql` |
| The consistency edge case, test written first | yes | `db/tests/40_test_consistency.sql` |
| — the genuinely concurrent version | yes | `db/run_concurrency.sh` |
| Week 1 exit test | yes | `db/tests/run_all.sql` |

**Deviation, documented:** `complete_session` does not deduct hours, because
`book_session` already reserved them. This was necessary to make the plan's own
cancellation rule work. Full reasoning in `design-decisions.md` §1.

**Added beyond the plan:** `PACKAGE_LEDGER` (append-only record of every hour
movement), `TUTORS` (the lock target for double-booking), `PKG_EVENTS`,
`PKG_OUTBOX`, `PKG_STUDENTS`.

## Week 2 — .NET API

| Item | Written | Where |
|---|---|---|
| ASP.NET Core Web API, .NET 8 | yes | `src/TutoringOps.Api/` |
| `Oracle.ManagedDataAccess.Core` | yes | `TutoringOps.Api.csproj` |
| Package calls via `CommandType.StoredProcedure` | yes | `Data/*Repository.cs` |
| No business rules in C# | yes | the repositories bind, call, commit or roll back |
| `POST /students`, `GET /students/{id}` | yes | `Controllers/StudentsController.cs` |
| `POST /sessions` | yes | `Controllers/SessionsController.cs` |
| `DELETE /sessions/{id}` | yes | same |
| `POST /sessions/{id}/complete` | yes | same |
| `POST /packages` | yes | `Controllers/PackagesController.cs` |
| `GET /students/{id}/balance` | yes | `Controllers/StudentsController.cs` |
| Result codes → HTTP (409 / 402 / 422) | yes | `Data/ResultCode.cs` |
| Outbox publisher as `IHostedService` | yes | `Outbox/OutboxPublisherService.cs` |
| Integration tests against Docker Oracle | yes | `src/TutoringOps.Api.Tests/` |
| Week 2 exit test as `.http` | yes | `TutoringOps.Api.http`, all failure cases included |

## Week 3 — Service Bus, Functions, Cosmos

| Item | Written | Where |
|---|---|---|
| Resource group and provisioning | yes | `infra/provision.sh` |
| Service Bus namespace, Standard tier | yes | same |
| Queue `session-events` | yes | same |
| Topic `session-notifications` | yes | same |
| Subscriptions `reminders` and `billing-updates` | yes | with a SQL filter on `reminders` |
| Publisher sends to both queue and topic | yes | `Outbox/EventPublisher.cs` |
| Function App, isolated worker, .NET 8 | yes | `src/TutoringOps.Functions/` |
| ServiceBusTrigger on `reminders`, email, Spanish when preferred | yes | `Functions/SessionReminderFunction.cs` + `Services/EmailTemplates.cs` |
| ServiceBusTrigger on `billing-updates`, upsert to Cosmos | yes | `Functions/BillingProjectionFunction.cs` |
| Timer trigger, nightly, queues reminders | yes | `Functions/NightlyReminderSweepFunction.cs` |
| Cosmos container `student-dashboards`, partition key `/studentId` | yes | `infra/provision.sh` |
| Read-model document per student | yes | `Models/StudentDashboardDocument.cs` |
| Written only by the Function App, read only by the UI | yes | one-way by construction |
| Week 3 exit test (watch the chain, screenshot it) | **not done** | needs a live Azure subscription |

## Week 4 — UI, deployment, stretch

| Item | Written | Where |
|---|---|---|
| Razor Pages app | yes | `src/TutoringOps.Web/` |
| Admin week calendar, book/cancel | yes | `Pages/Schedule.cshtml` |
| Per-student balances | yes | `Pages/Students.cshtml` |
| Parent status page, reads Cosmos | yes | `Pages/Status.cshtml` |
| Code-based access, not full identity | yes | session cookie + per-student code |
| App Service plan B1 | yes | `infra/provision.sh` |
| API and UI as Web Apps | yes | same |
| Function App on Consumption | yes | same |
| Oracle stays self-hosted, reached by tunnel | yes, decided | `design-decisions.md` §9; host is the Ubuntu server |
| GitHub Actions deploying on push to main | yes | `.github/workflows/deploy.yml` |
| Settings and secrets in Azure config | yes | `provision.sh`; nothing secret is committed |
| **Stretch:** APIM | not built | — |
| **Stretch:** Event Grid on package-exhausted | not built | the `PackageExhausted` event is already emitted, so the handler has something to bind to |
| **Skipped on purpose:** Data Factory, Service Fabric | — | no use for either |
| Week 4 exit (a real student's schedule running through it) | **not done** | needs the system deployed and a real family using it |

**Beyond the plan:** CI (`.github/workflows/ci.yml`) runs the PL/SQL suite and
the concurrency suite against a real Oracle container on every push, then builds
and tests the .NET solution against the same database.

---

## The resume entry

Drafted, with each line marked for what it currently supports. **Do not use a
line until the thing it describes has actually run.**

> **Tutoring Operations Platform** | C#, ASP.NET, Oracle PL/SQL, Azure (Service
> Bus, Functions, Cosmos DB, Web Apps)
>
> - Built a scheduling and billing system for an active tutoring business:
>   transactional data in Oracle with business validation in PL/SQL packages
>   (booking conflicts, package hour accounting, cancellation rules)
> - Built event-driven processing with Azure Service Bus queues and topics
>   feeding .NET Function Apps for reminders and billing updates, with a Cosmos
>   DB read model serving the client dashboard
> - Deployed to Azure Web Apps with GitHub Actions CI/CD

| Line | Supported when |
|---|---|
| Line 1 | `./db/run_tests.sh` and `./db/run_concurrency.sh` pass, and `dotnet test` passes |
| Line 2 | a booking made locally has been seen to land in Service Bus, fire both functions, and update the Cosmos document |
| Line 3 | the deploy workflow has run green and the deployed URLs respond |
| "for an active tutoring business" | at least one real student's schedule is in it |

**Interview sentence:** "I run a tutoring business on the side and built its
operations system on the same pattern your team uses: business logic in PL/SQL,
event-driven processing behind a .NET API."

**Claim boundaries.** The Oracle claim is "built with Oracle and PL/SQL", never
Oracle at enterprise scale. Nothing here has been under load, and the schema has
never held more than seed data. The strongest honest technical claim is the
concurrency work: the invariant that hours cannot go negative or be restored
twice is enforced by constraints and row locks, and there is a test that proves
it with six concurrent sessions.
